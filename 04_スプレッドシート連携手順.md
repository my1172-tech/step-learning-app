# 04 スプレッドシート連携（GAS Web App＋ID/パスワード認証）設定手順

児童生徒はID＋パスワードでログインしてから学習し、結果を送信します。
アプリが送るのは **IDとログイン券** だけです。氏名はスプレッドシート側にのみ存在します。

---

## 全体の流れ

```
生徒の端末（index.html）
   │ ① ID＋パスワードのハッシュをPOST（パスワードはURLに載せない）
   │ ② ログイン結果をJSONPで受け取る → ログイン券を保持（当日中有効）
   ↓ ③ 学習後、ID＋ログイン券＋回答をPOST
GAS Web App
   │ 券を検証 → 名簿でIDを照合
   ↓
回答シートに1問1行で追記
   ↑ ④ 到着件数をJSONPで確認 → 端末に「送信できました」
```

**なりすまし防止の要点**：ログイン券はサーバ側で `SECRET＋ID＋日付` から再計算して照合します。保存しないので、他人のIDで送ろうとしても券が一致せず `NG:ticket` で弾かれます。

---

## 1. スプレッドシートとスクリプト

1. Googleドライブで新しいスプレッドシートを作成（例：`ステップ学習_集約`）。
2. 拡張機能 → **Apps Script**。既定のコードを全部消し、`コード.gs` を貼り付ける。
3. 先頭の2つを書き換える。
   - `TOKEN`：合言葉。`index.html` の `SEND_TOKEN` と同じ文字列。
   - `SECRET`：**長くて推測されない文字列**。ログイン券の生成に使うため、外部に出さない。
4. 保存し、関数一覧から **`setup`** を実行（初回は承認が必要）。
5. `名簿` と `回答` の2シートができる。

---

## 2. 名簿とパスワード

`名簿` シートに1人1行で入力します。**氏名の列はありません。クラウド側に氏名は保存しません。**

| ID | 学年 | 組 | 出席番号 | パスワード |
|---|---|---|---|---|
| 2026-3A-12 | 3 | A | 12 | sakura7 |
| 2026-3A-13 | 3 | A | 13 | umeboshi3 |

氏名との対応表は、先生の端末に別のCSVとして保管し、`roster.html` で突き合わせます（06の手順）。

- パスワードは**平文のままでも動きます**。GAS側でハッシュ化して照合します。
- より安全にするなら、メニューから **`パスワードをハッシュ化`** を実行してください。**元には戻せない**ため、配布用の一覧を別に印刷してから実行してください。
- ハッシュ化後にパスワードを変えるときは、そのセルに新しい平文を上書きし、再度この関数を実行します。

**パスワードの決め方**：数字4桁は避けてください。単語＋数字（例：`sakura7`）程度で十分です。目的は他人になりすまさないことであり、外部からの攻撃対策ではありません。

## 3. Web Appとして公開する

1. 右上 **デプロイ → 新しいデプロイ** → 種類：**ウェブアプリ**。
2. 次のユーザーとして実行：**自分**／アクセスできるユーザー：**全員**。
3. `.../exec` で終わるURLをコピー。

> **「全員」が選べない場合**：組織のポリシーで外部公開が制限されています。この方式は使えません。
> **コードを直したら**：デプロイを管理 → 編集 → バージョン：**新バージョン** → デプロイ。これを忘れるとURLは古いコードのまま動きます。最も多い失敗です。

---

## 4. アプリ側に設定する

`index.html` の先頭近くの2行を書き換えます。

```js
const SEND_URL   = "https://script.google.com/macros/s/××××××/exec";
const SEND_TOKEN = "（自分で決めた合言葉）";
```

`SEND_URL` を入れると、ホーム画面にログイン欄が出て、**ログインするまで「学習をはじめる」が押せなくなります**。空のままなら、ログインも送信もない従来どおりの動作です。学級によって使い分けられます。

---

## 5. 動作確認

1. `index.html` を開き、名簿の1件でログイン。「3年A組12番 でログイン中です」と出れば成功。
2. サンプル問題で2〜3問学習し、「先生に送信」。
3. 「送信できました（3件）」と出て、`回答` シートに行が増えれば完了。

---

## 6. ログインの有効期限

| 項目 | 仕様 |
|---|---|
| 有効期間 | ログインした当日中（日付が変わると再ログイン） |
| 送信の受付 | 当日と前日の券を受け付ける（授業が日をまたいだ場合の救済） |
| 端末に残るもの | ID・ログイン券・「3年A組12番」の表示文字列のみ。**氏名はどこにも保存されません** |
| ログアウト | ホーム画面のボタンで即時に消える。共用端末では授業終わりに押させる |

---

## 7. 集計する

`回答` シートは1問1行です。ピボットテーブルで次のように見られます。

| 見たいこと | 行 | 列 | 値 |
|---|---|---|---|
| 誰がどこでつまずいたか | ID | 単元 | 正誤の個数（氏名を出すには `roster.html` を使う） |
| STEPごとの到達度 | 組 | STEP | 正解数の合計÷設問数の合計 |
| 時間のかかった問題 | 問題ID | ― | 所要秒の平均 |

---

## 8. この認証で守れること・守れないこと

| | |
|---|---|
| **守れる** | 他人のIDで結果を送ること。ログインしていない端末からの送信。名簿にないIDからの送信（券が発行されない） |
| **守れない** | アプリの利用そのもの。`index.html` は生徒の端末にあるため、コードを開けばログイン処理は迂回できます。**学習内容の保護にはなりません** |
| **守れない** | パスワードの共有。生徒同士が教え合えば成立します。運用で指導してください |

`index.html` を読めば `SEND_URL` と `SEND_TOKEN` は分かります。**校外に配布しないでください**。`SECRET` はGAS側にしかないため、券の偽造はできません。

---

## 9. うまくいかないとき

| 症状 | 原因と対処 |
|---|---|
| 「ログインの確認ができませんでした」 | URLが違う／アクセス範囲が「全員」でない／新バージョンでデプロイしていない |
| 「IDかパスワードがちがいます」 | 名簿の値と不一致。前後の空白、全角半角に注意 |
| 「しばらく待ってからやり直してください」 | 同じIDで5回失敗。10分後に解除される |
| ログインはできるが送信で件数が合わない | 日付が変わって券が失効。ログアウト→再ログイン |
| 行は増えるが氏名が「未登録」 | 名簿のIDと送信されたIDが不一致 |
| 何も起きない | `SEND_URL` が空。`index.html` の書き換えを確認 |
