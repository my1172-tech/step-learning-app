# 01 利用マニュアル

ファイルは2つです。役割が違うので、配布先を間違えないでください。

| ファイル | 配布先 | できること |
|---|---|---|
| `index.html` | 児童生徒 | ログイン → 課題をえらぶ → 学習 → 送信 |
| `teacher.html` | **先生のみ** | 課題の作成・編集・配信停止、問題プロンプト作成 |
| `roster.html` | **先生のみ** | ID→氏名の突き合わせと集計。通信しない |

どちらも使う前に、ファイル先頭の `SEND_URL` と `SEND_TOKEN` を設定してください（04の手順）。設定しないと動きません。

---

## 児童生徒の操作

1. `index.html` を開く
2. IDとパスワードを入れて **ログイン**
3. 「課題をえらぶ」から課題を選び「はじめる」
4. 答えを選ぶ → 説明の空らんを選ぶ → 「回答」
5. 最後まで進むと結果画面。**「先生に送信」**を押す
6. 「送信できました（◯件）」と出れば完了

出題する教科・単元・問題数・出題順は、先生が課題で指定した内容が自動で適用されます。生徒が範囲を選ぶ操作はありません。

ログインは当日中有効です。日付が変わると再ログインが必要になります。共用端末では授業の終わりに「ログアウト」を押させてください。

---

## STEPの構成

| STEP | 児童生徒の操作 | ねらい |
|---|---|---|
| 1 | 答えを4択から選ぶ | 知識・理解 |
| 2 | 答えを選ぶ → その理由の説明文を4択から選ぶ | 説明を選び取る |
| 3 | 答えを選ぶ → 説明文の空らん（１）を4択から選ぶ | 説明を組み立てる |
| 4 | 答えを選ぶ → 説明文の空らん（１）（２）を各4択から選ぶ | 説明を最後まで組み立てる |

答えと空らんが**すべて正解のときだけ「正解」**になります。部分正解は「2/3か所」の形で記録に残ります。

---

## 先生がつくる問題CSVの仕様

Driveの問題フォルダに置くCSVの列です。Excelで作り、**「名前を付けて保存 → CSV UTF-8」**で保存してください。Shift-JISでも読めます。

| 列名 | 必須 | 内容 |
|---|---|---|
| 問題ID | 任意 | 連番。空なら自動採番 |
| 学年 | **必須** | 1〜9（1〜6=小学校、7〜9=中学校）。「小5」「中2」でも可 |
| 教科 | **必須** | 国語／算数／数学／英語 |
| 単元 | **必須** | 課題側の単元指定と突き合わせる文字列 |
| STEP | **必須** | STEP1〜STEP4（数字だけでも可） |
| 問題文 | **必須** | 出題する文 |
| 選択肢A〜D | **必須** | 全STEPで必要。2つ以上あれば動く |
| 正解 | **必須** | A〜D、1〜4、ア〜エ、または選択肢と同じ文字列 |
| 説明文 | STEP3・4で必須 | 空らんを（１）（２）で示した文。①②や(1)(2)でも可 |
| 空欄1選択肢 | STEP2〜4で必須 | 縦棒区切り `0.25\|2.5\|25\|0.025` |
| 空欄1正解 | STEP2〜4で必須 | 選択肢と同じ文字列、またはA〜D |
| 空欄2選択肢／空欄2正解 | STEP4で必須 | 同上 |
| 模範解答 | 任意 | 補足を出したいときだけ |
| 解説 | 推奨 | 回答後に表示 |
| タグ | 任意 | 観点の記録用 |

- STEP2は説明文を空にし、空欄1選択肢に「説明そのもの」を4つ並べます。
- 列の順番は入れ替え可。1行目の見出し名で判定します。
- **選択肢の中に縦棒（|）は使えません**。区切り文字に使っているためです。
- 選択肢・正解・空欄がそろっていない行は読み飛ばされます。

`サンプル問題.csv` が書式の見本です。

---

## 課題の教科・単元と、CSVの中身の関係

課題で教科・単元を指定すると、その条件に合う問題だけが出題されます。
**一致する問題が1問もない場合は、そのCSVの全問から出題します**。課題側の入力ミスで出題ゼロになり授業が止まるのを避けるためです。単元を絞りたい場合は、CSVの単元欄と課題の単元欄の文字列を完全に一致させてください。

---

## うまくいかないとき

| 症状 | 対処 |
|---|---|
| ログイン画面が操作できない | `SEND_URL` が未設定。04の手順を確認 |
| 「IDかパスワードがちがいます」 | 名簿の値と不一致。前後の空白、全角半角に注意 |
| 画面に名前が出ない | 仕様です。氏名はクラウドに保存しません。学年組番号とIDのみ表示します |
| 課題が出てこない | 対象学年・組が名簿と不一致／公開開始が未来／配信停止／しめきり+7日経過 |
| 送信の件数が合わない | 日付が変わって券が失効。ログアウト→再ログイン |
| 送信できない | 「結果をCSVで保存」で先生に提出。あとで先生がシートに追記できる |
