# 別紙1　構築手順書（情報担当用）

ステップ学習アプリの、スプレッドシート作成からApps Script設置・公開までの手順である。
所要時間はおよそ1〜2時間。1回だけ実施すればよい。

**この手順を始める前に、必ず「0. 事前確認」を済ませること。** 確認を怠ると、最後まで作業してから導入不可と判明する。

---

## 0. 事前確認

| # | 確認事項 | 確認方法 | 不可の場合 |
|---|---|---|---|
| 0-1 | Apps Scriptのウェブアプリを「全員」に公開できるか | 手順5で選択肢が選べるかを見る。組織ポリシーで禁止されている場合がある | **導入不可**。CSV提出方式に切り替える |
| 0-2 | 作業アカウントでDriveにフォルダを作成できるか | Driveで新規フォルダを作成 | 権限のあるアカウントで実施する |
| 0-3 | 児童生徒の端末でHTMLファイルを開けるか | 任意のHTMLをダウンロードして開く | 端末ポリシーの確認が必要 |
| 0-4 | 生成AIを校務で利用できるか | 各自治体の運用ルールを確認 | 問題作成は手作業になる |

**0-1が最重要。** ここが通らなければ以降の作業は無駄になる。先に確認すること。

---

## 1. 準備するもの

| 物 | 備考 |
|---|---|
| 作業用のGoogleアカウント | **個人のアカウントは避ける**。異動時にデータごと失われる |
| 配布ファイル一式 | `コード.gs` `index.html` `teacher.html` `roster.html` `サンプル問題.csv` |
| 学級名簿 | ID・学年・組・出席番号を作るための元データ |
| 合言葉と秘密鍵 | 手順2で決める。控えを残す |

**推奨**：スプレッドシートとDriveフォルダは、個人のマイドライブではなく**共有ドライブ**に置く。作成者の異動・退職でデータが失われることを防ぐ。

---

## 2. スプレッドシートとApps Scriptの設置

### 2-1. スプレッドシートを作る

1. Driveで新規スプレッドシートを作成する。
2. 名前を `ステップ学習_集約` とする。
3. 共有設定を**教職員のみ**にする。児童生徒には共有しない。

### 2-2. スクリプトを貼る

1. スプレッドシートのメニューから **拡張機能 → Apps Script** を開く。
2. 既定の `コード.gs` の中身を**すべて削除**する。
3. 配布された `コード.gs` の内容を貼り付ける。
4. プロジェクト名を `ステップ学習` などに変更する（任意）。

### 2-3. 3つの定数を設定する

ファイル冒頭の次の3行を書き換える。

```js
const TOKEN  = "（自分で決めた合言葉）";               // 合言葉
const SECRET = "hakone-2026-taku-secret";  // 秘密鍵
const QUESTION_FOLDER_ID = "";             // 問題フォルダのID（手順3で取得）
```

| 定数 | 決め方 | 注意 |
|---|---|---|
| `TOKEN` | 学校で決めた任意の文字列 | `index.html` `teacher.html` の `SEND_TOKEN` と**完全に一致**させる。ファイルを見れば読めるので秘密ではない |
| `SECRET` | **長くランダムな文字列**。下記の方法で生成する | **絶対に外部へ出さない**。これだけがログイン券の偽造を防いでいる。配布サンプルのまま使ってはならない |
| `QUESTION_FOLDER_ID` | 手順3で取得 | 空欄のままだと問題ファイルの選択肢が出ない |

**秘密鍵の生成方法**：スクリプトエディタに次を貼り、関数一覧から `秘密鍵を作る` を実行し、実行ログに出た文字列を `SECRET` に貼る。貼り終えたらこの関数は削除してよい。

```js
function 秘密鍵を作る() {
  Logger.log(Utilities.getUuid() + "-" + Utilities.getUuid());
}
```

`SECRET` を後から変更すると、発行済みのログイン券がすべて無効になる。運用開始前に確定させること。

### 2-4. シートを作る

1. 保存する（Ctrl+S）。
2. 関数一覧から **`setup`** を選び、実行する。
3. **初回は承認画面が出る。**
   - 「権限を確認」→ アカウントを選択
   - 「このアプリは確認されていません」と出たら、**詳細 → （プロジェクト名）に移動（安全ではないページ）** を選ぶ
   - スプレッドシートとDriveへのアクセスを許可する
4. 「シートを用意しました」と表示されれば成功。

スプレッドシートに次の4シートができる。

| シート | 列 |
|---|---|
| 名簿 | ID／学年／組／出席番号／パスワード |
| 教員 | 教員ID／氏名／パスワード |
| 課題 | 課題ID／課題名／教科／単元／問題ファイル／対象学年／対象組／公開開始／締切／出題数／出題順／再挑戦／担当／有効／登録日時 |
| 回答 | 送信日時／バッチID／課題ID／課題名／時間外／ID／学年／組／出席番号／問題ID／学年(問題)／教科／単元／STEP／問題文／えらんだ答え／えらんだ説明／正誤／正解数／設問数／所要秒 |

**名簿に氏名の列はない。** 意図した設計であり、追加してはならない。

---

## 3. Driveの問題フォルダ

### 3-1. 変換設定をオフにする

**この作業を先に行う。** 後から行うと、既にアップロードしたCSVが変換済みのままになる。

1. Driveを開き、右上の **歯車 → 設定**。
2. 「アップロードしたファイルを Google ドキュメント エディタ形式に変換します」の**チェックを外す**。

チェックが入ったままCSVをアップロードすると、Googleスプレッドシートに変換され、アプリから中身を読めなくなる。名前に `.csv` が残るため一覧には出てしまい、原因が分かりにくい。

### 3-2. フォルダを作りIDを取得する

1. Driveで `ステップ学習_問題` フォルダを作成する。
2. フォルダを開き、URLの `/folders/` の後ろをコピーする。

```
https://drive.google.com/drive/folders/1AbC2DeF3GhI4JkL5MnO
                                        └──── これがID ────┘
```

3. Apps Scriptに戻り、`QUESTION_FOLDER_ID` に貼り付けて保存する。
4. `サンプル問題.csv` をこのフォルダの**直下**にアップロードする（動作確認用）。

**共有設定**：教員は編集者、**児童生徒には共有しない**。アプリはApps Script経由で読むため、児童生徒に権限は不要である。共有すると問題が直接閲覧できてしまう。

---

## 4. 名簿と教員の登録

### 4-1. 教員シート

| 教員ID | 氏名 | パスワード |
|---|---|---|
| fukushima | 福島 | （児童生徒用とは別の強度にする） |

### 4-2. 名簿シート

| ID | 学年 | 組 | 出席番号 | パスワード |
|---|---|---|---|---|
| 2026-3A-12 | 3 | A | 12 | sakura7 |

- IDは**年度＋学級＋番号**の形式を推奨する。出席番号だけでは他学級と重複する。
- パスワードは数字4桁を避ける。単語＋数字（`sakura7` 程度）でよい。目的はなりすまし防止であり、外部攻撃対策ではない。
- **氏名とIDの対応表**は、別のCSVとして教員端末に保管する（`roster.html` で使用）。成績データと同等に扱う。

### 4-3. パスワードのハッシュ化（任意）

平文のままでも動作する。より安全にする場合のみ実施する。

1. **先に配布用の一覧を印刷する。**
2. メニューから `パスワードをハッシュ化` を実行する。
3. 名簿と教員の両方の平文がハッシュに置き換わる。**元には戻せない。**

変更時は、該当セルに新しい平文を上書きし、再度この関数を実行する。

---

## 5. ウェブアプリとして公開

1. Apps Script右上の **デプロイ → 新しいデプロイ**。
2. 種類の選択（歯車アイコン）→ **ウェブアプリ**。
3. 次のとおり設定する。

| 項目 | 設定値 |
|---|---|
| 説明 | `v1` など任意 |
| 次のユーザーとして実行 | **自分** |
| アクセスできるユーザー | **全員** |

4. デプロイを押し、表示された **ウェブアプリのURL**（`/exec` で終わる）をコピーする。

> **「全員」が選べない場合**：組織ポリシーで制限されている。この方式は使用できない。管理者に許可を依頼するか、CSV提出方式へ切り替える。

> **重要**：**2回目以降は「新しいデプロイ」を使わない。** URLが変わり、配布済みのファイルがすべて動かなくなる。更新は手順8の方法で行う。

---

## 6. HTMLファイルへの埋め込み

`index.html` `teacher.html` `roster.html` のうち、**前2つ**をメモ帳等で開き、先頭付近の2行を書き換える。

```js
const SEND_URL   = "https://script.google.com/macros/s/××××××/exec";
const SEND_TOKEN = "（自分で決めた合言葉）";
```

| ファイル | 書き換え | 備考 |
|---|---|---|
| `index.html` | 必要 | 児童生徒用 |
| `teacher.html` | 必要 | 教員用。**同じ値**にする |
| `roster.html` | 不要 | 通信しないため設定項目がない |

**注意**：引用符（`"`）を消すと構文エラーとなり、画面が真っ白になる。書き換え後は必ずブラウザで開いて確認する。

---

## 7. 動作確認

上から順に実施する。途中で失敗したら、その項の対処を行ってから次へ進む。

| # | 確認 | 期待される結果 | 失敗時の対処 |
|---|---|---|---|
| 7-1 | ブラウザで `……/exec?action=verify&batch=x` を開く | `{"ok":false,"count":0}` が表示される | ログイン画面やエラーなら、アクセス範囲が「全員」でない |
| 7-2 | `teacher.html` を開く | ログイン画面が表示される | 白紙なら `SEND_URL` の書き換えミス |
| 7-3 | 教員IDでログイン | 「◯◯ 先生」と表示される | 教員シートの値、`TOKEN` の一致、デプロイの更新を確認 |
| 7-4 | 問題ファイルの選択肢を見る | `サンプル問題.csv` が出る | `QUESTION_FOLDER_ID` 未設定、Drive承認未実行、変換されている |
| 7-5 | 課題を1つ作成する | 一覧に表示される | 保存の確認に失敗する場合はデプロイの更新漏れ |
| 7-6 | `index.html` で児童生徒IDでログイン | 「3年A組12番（ID）」と表示される | 名簿の値、対象学年・組の一致を確認 |
| 7-7 | 課題一覧を見る | 7-5で作った課題が出る | 対象学年・組、公開開始、配信欄を確認 |
| 7-8 | 2〜3問解いて「先生に送信」 | 「送信できました（◯件）」 | 件数不一致ならログイン券の失効。再ログイン |
| 7-9 | スプレッドシートの回答シート | 行が増えている | `NG` の場合は下表を参照 |
| 7-10 | `roster.html` で氏名を突合 | 氏名つきの明細が出る | 対応表の見出しに `ID` と `氏名` があるか確認 |

---

## 8. 更新手順

| 変更した箇所 | 必要な作業 |
|---|---|
| 問題CSV | Driveのファイルを差し替えるだけ。**ファイル名は変えない**（課題が壊れる） |
| 課題の設定 | `teacher.html` から編集するだけ |
| 名簿・教員 | シートを編集するだけ |
| **`コード.gs`** | **デプロイを管理 → 編集（鉛筆）→ バージョン：新バージョン → デプロイ**。URLは変わらない |
| `index.html` `teacher.html` | 配布し直す。児童生徒に再ダウンロードさせる |

**最も多い失敗**：コードを直したのに「新バージョン」でデプロイせず、旧コードのまま動いているケース。動作がおかしいときは、まずここを疑う。

---

## 9. エラーコード一覧

提出や課題保存が反映されない場合、原因は次のいずれかである。

| コード | 意味 | 対処 |
|---|---|---|
| `NG:token` | 合言葉の不一致 | `コード.gs` の `TOKEN` とHTMLの `SEND_TOKEN` を突き合わせる |
| `NG:ticket` | ログイン券が無効 | 日付をまたいで失効した。再ログインする。他人のIDでの提出でも発生する |
| `NG:empty` | 送信データが空 | 学習記録がない状態で送信した |
| `NG:json` | データの形式異常 | 通信の途中断。再送する |
| `NG:busy` | 同時アクセスの競合 | 30秒待って再送する |
| `NG:action` | 不明な要求 | HTMLとコードのバージョン不一致。両方を最新にする |

画面表示のうち「ログインの確認ができませんでした」は、URL誤り・アクセス範囲・デプロイ未更新のいずれかである。

---

## 10. 年度更新と引き継ぎ

| 作業 | 内容 |
|---|---|
| 名簿の更新 | 名簿シートを新年度のIDに差し替える。旧年度分は別シートに退避 |
| 問題の整理 | Driveの問題フォルダに `_archive_2026` を作り、旧CSVを移動する。サブフォルダは読まれないため、選択肢から自動的に消える |
| 課題の停止 | 旧年度の課題は `有効` 列を `×` にする |
| 対応表の保管 | 氏名とIDの対応表を年度ごとに保管する。過年度データの突合に必要 |
| 引き継ぎ | 下記の設定値記録表を更新し、後任へ渡す |

---

## 付録　設定値記録表

引き継ぎのため、実際の値を記入して保管すること。**`SECRET` を含むため、この表は施錠管理する。**

| 項目 | 値 |
|---|---|
| スプレッドシートのURL | |
| 作業アカウント | |
| `TOKEN`（合言葉） | |
| `SECRET`（秘密鍵） | ※外部へ出さない |
| `QUESTION_FOLDER_ID` | |
| ウェブアプリURL（`/exec`） | |
| デプロイのバージョン・更新日 | |
| 氏名対応表の保管場所 | |
| 設置日・設置者 | |
