# 03 配布手順

配布物は用途で分かれます。**`teacher.html` を生徒に配らないでください。**

| ファイル | 配布先 | 備考 |
|---|---|---|
| `index.html` | 児童生徒 | `SEND_URL` `SEND_TOKEN` 設定済みのものを配る |
| `teacher.html` | 先生のみ | 同上。生徒に渡ると課題を操作される |
| `roster.html` | 先生のみ | 氏名の対応表を扱う。生徒に渡さない |
| `サンプル問題.csv` | 先生 | 問題CSVの書式見本 |
| `コード.gs` | 情報担当 | スプレッドシートのApps Scriptに貼る |
| 01／04／05 | 先生・情報担当 | 手順書 |

---

## Teamsで配る（既定）

1. 学年または学級のチーム →「ファイル」→「アップロード」で `index.html` を置く。
2. 「投稿」で案内する。文例：

> 学習アプリを置きました。
> ①「ファイル」→ ステップ学習アプリ → index.html を**ダウンロード**
> ② ダウンロードした index.html をダブルクリックして開く
> ③ IDとパスワードでログイン

3. 問題の差し替えはDriveの問題フォルダで行います。**アプリの再配布は不要**です。
4. `teacher.html` は職員用チーム、または先生へ個別に配布します。

**注意**：SharePoint / Teams 上のHTMLは、クリックしてもブラウザで実行されずダウンロードになるのが既定の動作です。「ダウンロードして開く」前提で案内してください。

---

## 端末別の開き方

| 端末 | 手順 |
|---|---|
| Windows 11 | ダウンロード → 右クリック →「プログラムから開く」→ Edge |
| Chromebook | ファイルアプリでダウンロード → ダブルクリック → Chromeで開く |
| iPad | ローカルHTMLが開けない場合がある。GAS等でのホスティングが必要（**未確認**：機種・OSバージョンによる） |

---

## 更新したとき

| 変更した箇所 | 必要な作業 |
|---|---|
| 問題CSV | Driveの問題フォルダに置き換えるだけ |
| 課題（対象・期間など） | `teacher.html` から編集するだけ |
| `コード.gs` | Apps Scriptで **新バージョン** としてデプロイし直す。忘れると旧コードのまま動く |
| `index.html` / `teacher.html` | 配布し直す。生徒に再ダウンロードさせる |

アプリ本体の再配布が必要になるのは最後の1行だけです。日常の運用でここに触ることはありません。

---

## 運用上の確認事項

- **通信**：GASのウェブアプリURLとだけ通信します。
- **個人情報**：氏名はクラウドに一切保存しません。氏名の対応表は先生の端末にだけ置き、`roster.html` で必要なときに突き合わせます。
- **問題の保護**：問題CSVはGAS経由で読むため、**Driveの問題フォルダを生徒に共有しないでください**。共有すると問題が直接閲覧できます。
- **URLの秘匿**：`index.html` を開けば `SEND_URL` と `SEND_TOKEN` は読めます。校外に配布しないでください。
