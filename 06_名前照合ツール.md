# 06 名前照合ツール（roster.html）

クラウド側に氏名を置かない構成のための、先生専用ツールです。**通信しません。**

---

## なぜこの構成にするか

| データ | 置き場所 |
|---|---|
| ID・学年・組・出席番号・パスワード | スプレッドシート（クラウド） |
| **氏名** | **先生の端末のCSVだけ** |

スプレッドシートが万一流出しても、そこから個人は特定できません。氏名を見る必要があるのは集計のときだけなので、その瞬間だけ先生の端末で突き合わせます。

---

## 準備：氏名の対応表をつくる

Excelで次の2列を作り、CSVで保存します。列はこの2つがあれば足り、順番や他の列の有無は問いません。

| ID | 氏名 |
|---|---|
| 2026-3A-12 | 佐賀 太郎 |
| 2026-3A-13 | 多久 花子 |

**保管場所**：校務ネットワークの、成績など個人情報を置いているフォルダに準じて扱ってください。名簿と同じ扱いです。Driveの問題フォルダには置かないでください。

---

## 使い方

1. スプレッドシートの `回答` シートを開き、**ファイル → ダウンロード → カンマ区切り形式(.csv)**。
2. `roster.html` を開く。
3. 「1. 名簿を読み込む」で氏名の対応表CSVを選ぶ。
4. 「2. 回答データを読み込む」で手順1のCSVを選ぶ。
5. 氏名つきの明細と、生徒別サマリーが表示される。
6. 必要に応じて **氏名つきCSV** または **生徒別サマリー** をダウンロード。

生徒別サマリーには、氏名・ID・学年・組・出席番号・課題数・問題数・正答数・正答率・時間外の件数が出ます。

---

## 名簿の保存について

「この端末に名簿を保存する」にチェックを入れると、次に開いたときも名簿が残ります。

| 端末 | 推奨 |
|---|---|
| 先生個人の校務端末 | チェックしてよい。毎回の読み込みが不要になる |
| **共用端末・持ち出し端末** | **チェックしない**。他の人が開くと氏名が見える |

保存した名簿は「保存した名簿を消す」で削除できます。ブラウザの閲覧履歴削除でも消えます。

---

## 仕様と注意

| 項目 | 内容 |
|---|---|
| 通信 | 一切しません。ファイルは端末の外に出ません |
| 文字コード | UTF-8とShift-JISを自動判定します |
| 名簿にないID | 「（名簿にありません）」と表示し、行は捨てません。転入や名簿更新漏れに気づけます |
| 同じIDが複数 | あとの行で上書きします。件数を画面に表示します |
| 明細の表示 | 画面は先頭200行まで。ダウンロードするCSVは全行です |
| 出力ファイル | UTF-8＋BOM付き。Excelでそのまま開けます |

---

## 運用上の注意

- ダウンロードした**氏名つきCSVは個人情報**です。ダウンロードフォルダに置きっぱなしにせず、所定の場所へ移すか削除してください。
- `roster.html` 自体には氏名は含まれません。ツールを配ること自体は問題ありませんが、生徒に渡す必要はありません。
- IDの付け方を年度で変える場合、過年度の回答データと突き合わせるには当時の対応表が必要です。年度ごとに対応表を保管してください。
