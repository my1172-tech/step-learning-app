# 05 課題配信の運用（複数の先生・複数教科）

先生が課題を作って配信し、生徒はログインして課題を選んで学習します。
**時間制限は「公開期間」のみ**です。1回あたりの制限時間はありません。

---

## 役割と画面

| 画面 | 使う人 | できること |
|---|---|---|
| `teacher.html` | 先生 | 課題の作成・編集・配信停止、問題ファイルの選択、問題プロンプト作成 |
| `roster.html` | 先生 | ID→氏名の突き合わせと集計（オフライン） |
| `index.html` | 児童生徒 | ログイン → 課題を選ぶ → 学習 → 送信 |
| スプレッドシート | 先生 | 名簿・教員・課題・回答の管理と集計 |

---

## 準備（1回だけ）

1. `コード.gs` の `TOKEN` `SECRET` を書き換える。
2. Driveに問題CSV用のフォルダを作り、URLの `/folders/` の後ろのIDを `QUESTION_FOLDER_ID` に設定する。
3. `setup` を実行。`名簿` `教員` `課題` `回答` の4シートができる。
4. `教員` シートに先生を登録する。

   | 教員ID | 氏名 | パスワード |
   |---|---|---|
   | fukushima | 福島 | 任意の文字列 |

5. ウェブアプリとしてデプロイし、URLを `index.html` と `teacher.html` の **両方** の `SEND_URL` に設定する。`SEND_TOKEN` も両方に同じ値を入れる。
6. `teacher.html` は**先生にだけ配布**します。生徒に配ると課題を作られます。

---

## 先生の作業

### 1. 問題CSVをDriveに置く
`5年_算数_割合.csv` のように、**学年_教科_単元** で命名してください。ファイル名がそのまま選択肢に出ます。

### 2. 課題をつくる
`teacher.html` を開いてログインし、フォームに入力して保存します。

| 項目 | 説明 |
|---|---|
| 課題名 | 生徒に見える名前 |
| 問題ファイル | Driveフォルダ内のCSVから選択 |
| 対象学年／対象組 | 空欄なら全員。組は `A,B` のようにカンマ区切り |
| 公開開始 | 空欄なら即時公開。この時刻まで生徒の一覧に出ない |
| しめきり | 過ぎても一覧には残り、提出は「時間外」として記録される |
| 出題数／出題順 | 生徒側の設定を先生が固定する |
| 再挑戦 | 「不可」にすると、提出済みの生徒の一覧から消える |
| 配信 | 「×」にすると即座に配信停止 |

複数の先生が同じ画面を使えます。担当者名は保存時に自動で記録され、`課題` シートで区別できます。

### 3. 集計する
`回答` シートに `課題ID` `課題名` `時間外` の列があります。ピボットテーブルで課題別・先生別に集計できます。

**氏名は記録されていません。** 氏名つきで見たい場合は、`回答` シートをCSVでダウンロードし、`roster.html` に読み込ませてください（06の手順）。

---

## 生徒の操作

1. `index.html` を開く
2. ID＋パスワードでログイン
3. 「課題をえらぶ」から課題を選び「はじめる」
4. 学習 → 結果画面で「先生に送信」

生徒側に問題の読み込み機能や範囲選択はありません。出題条件は課題の設定だけで決まります。

先生が指定した教科・単元・出題数・出題順は自動で適用されます。

---

## 設計上の判断

| 項目 | 内容と理由 |
|---|---|
| 締切判定 | **サーバの時刻**で判定します。端末の時計を変えても効きません |
| 時間外の扱い | 拒否せず「時間外」として記録します。通信不調で数分遅れた生徒の記録を消すと事故対応ができないためです |
| 締切後の表示 | 締切後7日間は一覧に残ります（`LATE_DAYS` で変更可）。それを過ぎると消えます |
| 提出済み判定 | 回答シートに同じ課題IDの行があるかで判定します |
| 保存の確認 | 応答が読めないため、保存後に一覧を取り直して反映を確かめています |

---

## この仕組みで守れないこと

- `index.html` を開けばログイン処理は迂回できます。守れるのは「他人のIDで提出すること」までです。
- `teacher.html` を生徒が入手し、教員IDとパスワードを知られれば課題を操作できます。教員パスワードは生徒用と別の強度にしてください。
- 公開開始前の課題は一覧に出ませんが、問題CSVそのものはDriveフォルダの共有設定に従います。**フォルダを生徒に共有しないでください**。アプリはGAS経由で読むため、生徒に共有権限は不要です。

---

## うまくいかないとき

| 症状 | 対処 |
|---|---|
| 問題ファイルの選択肢が空 | `QUESTION_FOLDER_ID` が未設定、またはフォルダにCSVがない |
| 「問題ファイルが見つかりません」 | 課題シートのファイル名とDrive上の名前が不一致。拡張子まで一致させる |
| 生徒に課題が出ない | 対象学年・組が名簿と不一致／公開開始が未来／配信が「×」／締切+7日を過ぎた |
| 保存を確認できない | デプロイを「新バージョン」で更新していない |
| 日付が正しく読まれない | 課題シートの日付セルは文字列でも日付型でも可。手入力なら `2026-09-01 08:30` の形式で |
