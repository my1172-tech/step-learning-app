# 07 生成AIと壁打ちして、この学習アプリを自分で作り直す

このリポジトリは「完成品を配って使ってもらう」ためのものではありません。
**自分の学校の事情に合わせて、生成AIと壁打ちしながら作り直す**ための設計資料です。

このファイルには、そのまま生成AIに貼れる指示文と、実際に作ってみて分かった落とし穴を書いてあります。
コードをそのまま使っても構いませんが、作り直したほうが自分で直せるものになります。

> **前提**：プログラミングの知識は不要です。必要なのは「作りたいものを日本語で説明できること」と、
> AIが出したものを開いて動かし、違っていたら言葉で伝え直すことです。
> 使うのは生成AI（ChatGPT / Claude / Gemini など）とブラウザ、そしてGoogleアカウントだけです。

---

## 0. 全体の流れ

**5つのブロックに分け、1ブロックずつ完成させます。** まとめて頼むと必ず破綻します。

| 順 | ブロック | 成果物 | 通信 | 目安 |
|---|---|---|---|---|
| 1 | 児童生徒画面（オフライン版） | `index.html` | なし | 半日 |
| 2 | 問題データ | 問題CSV | なし | 1時間 |
| 3 | サーバー | `コード.gs` ＋ スプレッドシート | あり | 半日 |
| 4 | 教員画面 | `teacher.html` | あり | 半日 |
| 5 | 名簿照合ツール | `roster.html` | なし | 2時間 |

**ブロック1と2だけで、授業では使えます。** 記録は手元のCSVに保存する形になります。
クラウド集約（ブロック3以降）は、それが必要になってから足してください。

---

## 1. まず決めること（設計の芯）

この5つを崩すと別物になります。AIに最初に伝え、途中で押し戻されても維持してください。

### 1-1. 「答え」ではなく「理由」を選ばせる

これがこのアプリの全部です。正誤判定アプリは無数にあるので、作る意味がありません。

| STEP | 児童生徒の操作 | ねらい |
|---|---|---|
| 1 | 答えを4択から選ぶ | 知識・理解の確認 |
| 2 | 答えを選び、**その理由の説明文**を4択から選ぶ | 説明を選び取る |
| 3 | 答えを選び、説明文の**空欄（１）**を4択から埋める | 説明を組み立てる |
| 4 | 答えを選び、説明文の**空欄（１）（２）**を4択から埋める | 説明を最後まで組み立てる |

答えと空欄が**すべて**正解のときだけ「正解」とします。部分正解は「2/3か所」の形で記録に残します。

### 1-2. 氏名をクラウドに保存しない

保存するのは ID・学年・組・出席番号だけです。氏名は教員端末のCSVにしか置きません。
集計で氏名が必要になる瞬間だけ、教員端末の中で突き合わせます（ブロック5）。

この判断は後から変えられません。**最初に決めてください。**

### 1-3. HTMLファイル1枚で動かす

インストール不要、ビルド不要、サーバー不要。ダブルクリックで開けば動くこと。
Teamsやドライブで配れば、それだけで配布が終わります。

### 1-4. 外部ライブラリを使わない

CDNもnpmも使いません。SHA-256も自分で書きます（AIに書かせます）。
理由は、学校の端末では外部読み込みが遮断されることがあり、**そのとき原因が分からないから**です。

### 1-5. 追加費用ゼロ

既存の Google Workspace の範囲だけで作ります。予算要求が発生した時点で、導入は数か月遅れます。

---

## 2. ブロック1：児童生徒画面（通信なし版）

**ここに一番時間をかけてください。** 学習体験の良し悪しは全部この画面で決まります。

### そのまま貼れる指示文

```text
HTMLファイル1枚で動く、小中学生向けの学習アプリを作ってください。
ブラウザでファイルを開くだけで動き、サーバーも外部ライブラリも使いません。
CSS・JavaScriptもすべて同じファイルの中に書いてください。

■ 何をするアプリか
児童生徒が問題を解きます。ただし答えを選ぶだけでなく、
「なぜその答えになるのか」という理由の説明も段階的に選ばせます。
STEPが上がるほど、説明を自分で組み立てる度合いが上がります。

STEP1：答えを4択から選ぶ。
STEP2：答えを4択から選んだあと、その理由の説明文全体を4択から選ぶ。
STEP3：答えを4択から選んだあと、説明文の中の空欄（１）を4択から選んで埋める。
STEP4：答えを4択から選んだあと、説明文の中の空欄（１）（２）をそれぞれ4択から埋める。

答えと空欄がすべて正解のときだけ「正解」とします。
部分正解は「2/3か所」のように記録します。

■ 問題データ
問題は19列のCSVで与えます。列は次の順です。
問題ID,学年,教科,単元,STEP,問題文,選択肢A,選択肢B,選択肢C,選択肢D,正解,
説明文,空欄1選択肢,空欄1正解,空欄2選択肢,空欄2正解,模範解答,解説,タグ

・正解は A/B/C/D のいずれかで入ります。
・空欄1選択肢と空欄2選択肢は、候補4つを縦棒（|）で区切って1つのセルに入れます。
・説明文には（１）（２）が全角で入ります。その位置を選択肢で置き換えて表示します。
・STEP1では説明文・空欄1・空欄2の列は空です。
・画面には CSVファイルを選ぶボタンを置いてください。

■ 画面の作り
・ホーム画面 → 学習画面 → 結果画面 の3画面を、1ファイル内で切り替えます。
・1問ずつ表示します。答えを選んでから説明を選ぶ、という順に段階的に出します。
・その場で正誤を出し、解説を表示します。
・最後に「何問中何問」と、問題ごとの結果一覧を出します。
・結果をCSVでダウンロードできるようにしてください（先生に渡すため）。
・小学生が読める文字サイズにし、タップの当たり判定を広くしてください。
・Chromebookでも動くようにしてください。

■ 守ること
・外部ライブラリ・CDN・フレームワークを使わない。素のHTML/CSS/JSだけ。
・CSVの文字コードはUTF-8とShift-JISの両方を読めるようにする（Excelの既定保存に対応）。
・CSVのセル内のカンマ、ダブルクォート、改行を正しく扱うパーサーを自分で書く。
  ライブラリは使わない。split(",") で済ませてはいけない。

まず全体のHTMLを1つ出してください。そのあと私が動かして、直したいところを伝えます。
```

### この段階でやること

1. 出てきたHTMLを保存して**ブラウザで開く**
2. `サンプル問題.csv` を読み込ませる（このリポジトリのものが使えます）
3. STEP1〜4を1問ずつ実際に解く
4. 違うところを**日本語で**伝える（「STEP3で空欄が2つ出てしまう」など）

**動かさずに次を頼まないこと。** AIは動かないコードを自信を持って出します。

---

## 3. ブロック2：問題データ

問題は生成AIに作らせます。**教員の仕事は、方向性の決定と点検です。**

`teacher.html` には、条件を入れると作問プロンプトを組み立てる機能が入っています。
作り直す場合も、この考え方だけは真似する価値があります。

### 作問プロンプトに必ず入れる要素

| 要素 | なぜ必要か |
|---|---|
| 出力はCSV本文のみ、あいさつや説明を書くな | 書かせると必ず前後に文章が付き、CSVが壊れる |
| 見出し行を一字も変えずに出せ | 列名がずれると読み込めない |
| コードブロック記号やMarkdownの表を使うな | バッククォート3つや縦棒の表で出してくる |
| 全行を必ず19列にせよ | 列を勝手に省略する |
| 1問1行、空行を入れるな | 見やすくしようとして改行を入れる |
| **この単元で実際に起きやすい誤りを列挙してから誤答を作れ** | 指定しないと「明らかに変な誤答」を作り、消去法で解けてしまう |
| 説明の正解に、問題の正解の言いかえを書くな | 「答えはBだから」という中身のない説明を作る |
| 同じ根拠の説明を同じブロックで2回使うな | 5問中4問が同じ理由になる |
| 空欄2は空欄1の言いかえにするな。条件・数量関係・判断の材料などを問え | 空欄2が空欄1のコピーになる |

### 点検すること（AIに任せてはいけない）

- 誤答が**実際に児童生徒が起こす誤り**になっているか
- 消去法で解けてしまわないか
- 説明の正解が、答えを見ただけで決まってしまわないか
- 学年の漢字・語彙として適切か

**この点検が、このアプリの品質の全部です。** 生成が速いぶん、点検を省くと質の低い教材が量産されます。

---

## 4. ブロック3：サーバー（Apps Script）

ここから通信が入ります。**ブロック1が完成してから着手してください。**

### そのまま貼れる指示文

```text
Google スプレッドシートに紐づく Google Apps Script を書いてください。
学習アプリ（ローカルのHTMLファイル）から呼び出され、
ログイン認証・課題配信・提出受付を行います。

■ シート構成（setup()関数で自動作成する）
名簿 : ID | 学年 | 組 | 出席番号 | パスワード     ※氏名の列は作らない
教員 : 教員ID | 氏名 | パスワード
課題 : 課題ID | 課題名 | 教科 | 単元 | 問題ファイル | 対象学年 | 対象組 |
       公開開始 | 締切 | 出題数 | 出題順 | 再挑戦 | 担当 | 有効 | 登録日時
回答 : 送信日時 | バッチID | 課題ID | 課題名 | 時間外 | ID | 学年 | 組 | 出席番号 |
       問題ID | 学年(問題) | 教科 | 単元 | STEP | 問題文 |
       えらんだ答え | えらんだ説明 | 正誤 | 正解数 | 設問数 | 所要秒

■ 重要な制約（ここを間違えると全部動きません）
HTMLファイルは file:// で開かれるため、fetch のレスポンスをJavaScriptから読めません。
そのため通信を次の2系統に分けてください。

・書き込み（ログイン要求・提出・課題保存）は doPost で受ける。
  クライアントは fetch(url, {method:"POST", mode:"no-cors"}) で投げ、応答は読まない。
・読み取りは doGet で受け、JSONP で返す。
  クエリに callback=関数名 があれば「関数名(JSON)」の形で、
  MimeType.JAVASCRIPT として返す。callback は英数字とアンダースコアだけか検証する。

ログインは応答が読めないので、次の2段構えにします。
1. クライアントが nonce（ランダム文字列）を作り、POSTでログイン要求を送る
2. サーバーは結果を CacheService に "login:" + nonce のキーで180秒だけ保存する
3. クライアントは doGet の action=loginResult&nonce=... をJSONPで取りに行く

■ 認証方式
・パスワードはクライアント側でSHA-256にハッシュ化し、POSTの本文で送る。
  URLには絶対に載せない（ブラウザ履歴とサーバーログに残るため）。
・名簿シートのパスワードは、平文でもハッシュでも受け付ける
  （64桁の16進数ならハッシュとみなす）。平文を一括ハッシュ化する関数も付ける。
・ログイン成功時は「ログイン券」を返す。
  券 = SHA-256(SECRET + "|" + 役割 + "|" + ID + "|" + 日付yyyyMMdd)
  券はどこにも保存しない。検証時に毎回再計算して比較する。
  当日と前日の2つを有効とする（日付をまたいだ学習を切らないため）。
・同一IDで5回ログインに失敗したら10分間ロックする（CacheServiceを使う）。

■ 定数（冒頭に置く。空のまま配布する）
TOKEN                : HTMLと共有する合言葉
SECRET               : ログイン券の署名鍵。長くランダムな文字列
QUESTION_FOLDER_ID   : 問題CSVを置くDriveフォルダのID
LATE_DAYS            : 締切後、何日まで「時間外」として受け付けるか

doPost の先頭で、TOKEN か SECRET が空なら "NG:setup" を返して処理を止めてください。
未設定のまま公開されると誰でも書き込めてしまうためです。

■ doPost の action
login    : 役割・ID・パスワードハッシュ・nonce を受け、結果をキャッシュに置く
submit   : ID・券・バッチID・回答行の配列を受け、回答シートに追記する
saveTask : 教員ID・券・課題情報を受け、課題シートを追加または更新する
不正なものは "NG:token" "NG:ticket" "NG:empty" "NG:json" "NG:busy" "NG:action"
のいずれかを、プレーンテキストで返してください。

■ doGet の action（すべてJSONPで返す）
loginResult  : nonce に対応するログイン結果
tasks        : ID・券を検証し、その児童生徒が対象の有効な課題一覧を返す
questions    : ID・券・課題IDを検証し、Driveの問題CSVを読んで問題を返す
teacherTasks : 教員ID・券を検証し、全課題一覧とDriveのCSVファイル一覧を返す
verify       : バッチIDを受け、回答シートに何行入ったかを返す（到着確認用）

■ その他
・回答シートへの追記と課題保存は LockService で排他する（30秒待って取れなければ NG:busy）。
・タイムゾーンは Asia/Tokyo。
・課題の対象学年・対象組で配信を絞る。締切を過ぎたものは LATE_DAYS の間だけ
  「時間外」の印を付けて受け付ける。
```

### 通信方式のまとめ（AIが必ず間違えるところ）

```text
【書き込み】index.html ──fetch(mode:"no-cors")──▶ doPost   ※応答は読めない
【読み取り】index.html ──script要素のsrcで呼ぶ（callback付き）──▶ doGet ※JSONPで読む

【ログイン】
  1. POST {action:"login", nonce:"a1b2..."}   → 応答は読めない
  2. サーバーが結果を cache["login:a1b2..."] に180秒保存
  3. GET ?action=loginResult&nonce=a1b2...&callback=cb → 結果を受け取る

【提出】
  1. POST {action:"submit", batch:"x9y8...", rows:[...]}
  2. GET ?action=verify&batch=x9y8...&callback=cb → 入った行数を照合
  3. 数が合わなければ「CSVで保存して先生に渡してください」と案内する
```

AIは高い確率で「fetchのレスポンスをJSONで読む」普通のコードを書きます。
`file://` から開いたHTMLではCORSで読めないので、**動かした瞬間に破綻します。**
そうなったら、上の図をそのまま貼って直させてください。

### デプロイ設定（ここで詰まる人が多い）

- ウェブアプリとして公開する
- **実行するユーザー：自分**
- **アクセスできるユーザー：全員**（＝URLを知っている人。だから合言葉と券が必要）
- **コードを直したら、毎回「新しいデプロイ」を作る。** 保存だけでは反映されません

---

## 5. ブロック4：教員画面

```text
教員用のHTMLファイル1枚を作ってください。ブロック3のApps Scriptと通信します。
通信方式（no-cors POST ＋ JSONP GET、nonceによるログイン）は児童生徒画面と同じです。

■ 機能
1. 教員ID・パスワードでログインする（パスワードはSHA-256にして本文で送る）
2. 課題の一覧を見る
3. 課題を新規作成・編集する
   （課題名、教科、単元、問題ファイル、対象学年、対象組、公開開始、締切、
     出題数、出題順、再挑戦の可否、有効/無効）
   問題ファイルは、Driveの問題フォルダにあるCSVから選ばせる
4. 配信を停止する（有効フラグを外す）
5. 問題生成プロンプトを組み立てる
   学年・教科・単元・難易度・問題数・開始する問題ID・STEP内訳・ねらい・
   「この単元で起きやすい誤り」を入力すると、
   生成AIにそのまま貼れる指示文をテキストエリアに出す。
   問題数からSTEP1〜4の内訳を自動で割り振る。
   誤りが未入力なら、教科名から一般的な誤りを補う。

■ 守ること
・このファイルは教員にしか配らない前提で作ります。
  ただし児童生徒の手に渡っても、教員パスワードがなければ何もできないようにしてください。
・外部ライブラリを使わない。
```

作問プロンプトの中身は、このリポジトリの `teacher.html` の `buildPrompt()` が実例です。
**あの指示文の細かさが必要**だと分かるまで、たぶん何度か作問をやり直すことになります。

---

## 6. ブロック5：名簿照合ツール

```text
HTMLファイル1枚のツールを作ってください。通信は一切しません。
外部にデータを送らないことを、画面上でも明示してください。

■ 目的
クラウドには氏名を保存していないため、記録には ID・学年・組・出席番号しかありません。
このツールは、先生の端末にある名簿CSV（ID と 氏名）と、
ダウンロードした回答CSVを、この端末の中だけで突き合わせ、氏名を付けて表示します。

■ 機能
1. 名簿CSV（IDと氏名の列があればよい。他の列があってもよい）を読み込む
2. 回答CSVを読み込む
3. IDで突き合わせ、氏名付きの一覧を出す
4. 生徒別に集計する（正解数・設問数・正答率）
5. 結果をCSVで保存する
6. 名簿はブラウザのlocalStorageに保存できるようにし、削除ボタンも付ける

■ 守ること
・fetch・XMLHttpRequest・外部リソースの読み込みを一切書かない。
・CSVはUTF-8（BOM付きも）とShift-JISを自動判別する。
  UTF-8として厳密デコードを試し、失敗したらShift-JISで読む。
・突き合わせできなかったIDは、件数と一覧を必ず表示する（黙って捨てない）。
```

---

## 7. データ仕様（そのままAIに渡せます）

### 問題CSV（19列）

| # | 列名 | 内容 |
|---|---|---|
| 1 | 問題ID | 連番 |
| 2 | 学年 | 1〜9（1〜6=小学校、7〜9=中学校） |
| 3 | 教科 | 算数・国語・理科など |
| 4 | 単元 | 割合、漢字など |
| 5 | STEP | STEP1〜STEP4 |
| 6 | 問題文 | 設問 |
| 7〜10 | 選択肢A〜D | 答えの候補4つ |
| 11 | 正解 | A / B / C / D |
| 12 | 説明文 | STEP3・4のみ。（１）（２）を全角で埋め込む |
| 13 | 空欄1選択肢 | 候補4つを縦棒区切り。STEP2では説明文全体の候補4つ |
| 14 | 空欄1正解 | 候補のうち正しいもの |
| 15 | 空欄2選択肢 | STEP4のみ。縦棒区切り |
| 16 | 空欄2正解 | STEP4のみ |
| 17 | 模範解答 | 記述の参考（STEP1で使う） |
| 18 | 解説 | 解いた直後に見せる説明 |
| 19 | タグ | 分類用（百分率、部首など） |

STEPごとに埋める列が違います。空でよい列を無理に埋めさせないでください。

| STEP | 説明文 | 空欄1選択肢/正解 | 空欄2選択肢/正解 |
|---|---|---|---|
| 1 | 空 | 空 | 空 |
| 2 | 空 | **説明文全体**の候補4つ | 空 |
| 3 | （１）を1か所含む | （１）の候補4つ | 空 |
| 4 | （１）（２）を各1か所 | （１）の候補4つ | （２）の候補4つ |

実例は `サンプル問題.csv` を見てください。

### スプレッドシート（4シート）

`00_企画書.md` の「7. データ設計」と `コード.gs` 冒頭のコメントに全列が載っています。

---

## 8. つまずきポイント集

実際に作って分かったものです。**先に読んでおくと数時間節約できます。**

| 現象 | 原因 | 対処 |
|---|---|---|
| 提出しても何も起きない、エラーも出ない | `no-cors` のPOSTは応答が読めない。AIが応答を読むコードを書いている | JSONPの `verify` で到着を確認する設計にする |
| ログインが必ず失敗する | 同じく応答が読めていない | nonce＋CacheService＋JSONPの2段構えにする |
| コードを直したのに動作が変わらない | デプロイを更新していない | **新しいデプロイ**を作り直す |
| 全リクエストが `NG:setup` | `TOKEN` か `SECRET` が空 | 2つの定数を設定してデプロイし直す |
| `NG:token` | HTMLの `SEND_TOKEN` とGASの `TOKEN` が不一致 | 前後の空白も含めて突き合わせる |
| `NG:ticket` | 券が失効（2日以上経過）、または他人のID | 再ログインさせる |
| CSVが読み込めない・文字化け | Excelの既定保存がShift-JIS | UTF-8とShift-JISの自動判別を入れる |
| CSVの途中で列がずれる | セル内のカンマや改行を単純分割で切っている | 自分でパーサーを書かせる |
| 生成した問題の誤答が明らかに変で、消去法で解ける | 「起きやすい誤り」をプロンプトで指定していない | 誤りを列挙してから誤答を作らせる |
| 5問中4問の説明が同じ理由になる | 指示していない | 「同じ根拠を2回使うな」を入れる |
| 空欄2が空欄1の言いかえになる | 指示していない | 空欄2で問う観点を列挙して指定する |
| 問題が19列にならない | AIが列を省略する | 「全行を必ず19列にせよ」を最重要ルールに書く |
| CSVの前後に説明文が付く | AIが親切にやる | 「出力はCSV本文のみ」を1番目のルールに書く |
| 外部ライブラリを使ったコードが出てくる | 既定でそうする | 毎回「素のHTML/CSS/JSだけ」と書く。長い会話では忘れられるので再指示する |

### AIとの付き合い方

- **1回に1ブロック。** まとめて頼むと、動かない大きなコードが出てきます
- **必ず動かしてから次へ。** AIは動かないコードを自信を持って出します
- **日本語で症状を伝える。** 「STEP3で空欄が2つ出る」で十分です
- **会話が長くなったら制約を再掲する。** 「外部ライブラリ禁止」は忘れられます
- **AIが「もっと良い方法があります」と言っても、設計の芯（第1章）は変えない**
- **「動きました」と言われても信じない。** 判断するのは自分が開いた画面です

---

## 9. 動作確認チェックリスト

### ブロック1（通信なし）
- [ ] ファイルをダブルクリックして開ける
- [ ] `サンプル問題.csv` を読み込める
- [ ] STEP1〜4がそれぞれ仕様どおりに出る
- [ ] 全部正解のときだけ「正解」になる。部分正解が「2/3か所」で出る
- [ ] 結果CSVを保存できる
- [ ] Chromebookで開ける

### ブロック3以降（通信あり）
- [ ] `TOKEN`・`SECRET`・`QUESTION_FOLDER_ID` を設定した
- [ ] 児童生徒としてログインできる
- [ ] 存在しないIDでログインできない
- [ ] 5回失敗するとロックされる
- [ ] 対象学年・組でない課題が出てこない
- [ ] 提出が回答シートに入る
- [ ] **回答シートに氏名の列がない**
- [ ] 締切後の提出に「時間外」が付く
- [ ] 教員画面から課題を作成・停止できる
- [ ] 名簿照合ツールが通信していない（開発者ツールのネットワークタブで確認）

---

## 10. 作らないもの（意図的に外したもの）

| 外したもの | 理由 |
|---|---|
| 記述式の採点 | 選択式に絞ったことが設計の核。記述はノートにやらせる |
| アプリの利用制限 | HTMLは児童生徒の端末にあるので、開けば迂回できる。**守れないものは守れると言わない** |
| リアルタイムの進捗表示 | 教員が見続ける運用は現実的でない |
| 児童生徒への氏名表示 | 「3年A組12番」で足りる |
| ログイン状態の長期保持 | 券は当日と前日のみ。端末共有時の事故を減らす |

**このアプリは、自分の言葉で説明を書けるようになるための踏み台です。それ自体が到達点ではありません。**
STEP4まで解けたあとに、ノートへ自分の言葉で書かせる運用を前提にしてください。

---

## 11. 公開するときの注意

作ったものをGitHubなどに公開するなら、次の3つを**必ず空にしてから**にしてください。

| ファイル | 変数 | 漏れると何が起きるか |
|---|---|---|
| `index.html` / `teacher.html` | `SEND_URL` | 誰でもあなたのエンドポイントを叩ける |
| `index.html` / `teacher.html` | `SEND_TOKEN` | 合言葉の検証を通過される |
| `コード.gs` | `SECRET` | **ログイン券を偽造され、他人のIDで提出できる** |

このリポジトリはすべて空で公開しています。設定値を書き込んだファイルはコミットしないでください。
